<%@ page import="java.sql.*" %>
<%!
    // ====== CONFIGURACION DE LA BASE DE DATOS (centralizada, solo se edita aqui) ======
    private static final String BD_URL = "jdbc:mysql://byphnwobld4cvct1pmbm-mysql.services.clever-cloud.com:3306/byphnwobld4cvct1pmbm?useSSL=false&serverTimezone=UTC&useUnicode=true&characterEncoding=UTF-8&connectionCollation=utf8mb4_unicode_ci";
    private static final String BD_USER = "uimzn49hetytrt5m";
    private static final String BD_PASS = "Cg2JtIEXoiI8ZTyPniLS";
    private static final String BD_DRIVER = "com.mysql.cj.jdbc.Driver";

    public Connection obtenerConexion() throws Exception {
        Class.forName(BD_DRIVER);
        return DriverManager.getConnection(BD_URL, BD_USER, BD_PASS);
    }

    public void cerrarConexion(Connection con) {
        if (con != null) {
            try { con.close(); } catch (SQLException e) { }
        }
    }
%>